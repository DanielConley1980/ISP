# ISP Project — Claude Instructions

## Deployment
After every code change, always:
1. Commit the changes with a descriptive message
2. Push to the current feature branch
3. Create a PR into `main` and merge it immediately (squash merge)

Do this automatically without asking for confirmation. The site deploys from `main`, so merging = publishing.
